# Campaign Studio (Vite). Chat-driven campaign builder; calls the BFF.
#
# `VITE_*` values are inlined at build time and are public.

VITE_API_BASE_URL=

# Dev only: where the Vite proxy sends `/api`.
BACKEND_URL=http://localhost:4000
